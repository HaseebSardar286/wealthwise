<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="4dp"
    android:paddingBottom="4dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <TextView
        android:id="@+id/textViewMessageText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_message_sent" 
        android:padding="10dp"
        android:textColor="@android:color/white"
        android:maxWidth="260dp" 
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="This is a sent message. It can be quite long and wrap to multiple lines." />
        <!-- TODO: Create drawable/bg_message_sent.xml (e.g., rounded rectangle with primary color) -->

    <TextView
        android:id="@+id/textViewMessageTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginTop="2dp"
        app:layout_constraintEnd_toEndOf="@+id/textViewMessageText"
        app:layout_constraintTop_toBottomOf="@+id/textViewMessageText"
        tools:text="10:30 AM" />

</androidx.constraintlayout.widget.ConstraintLayout>
